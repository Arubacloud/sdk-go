version: "2"

run:
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unparam
    - unconvert
    - goconst
    - gocyclo
  settings:
    errcheck:
      check-blank: true
  exclusions:
    paths:
      - pkg/generated
      - cmd/example
    rules:
      - linters:
          - errcheck
        text: httpResp.Body.Close
      - path: _test\.go
        linters:
          - errcheck
          - goconst
          - gocyclo
formatters:
  enable:
    - gofmt
    - goimports
